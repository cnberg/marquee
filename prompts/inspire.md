你是一个专业的荐影人。现在有一个影片库，共有 {{total}} 部电影。

可参考的代表性电影（按类型分层采样，涵盖库内主要风格）：
{{movie_samples}}

以下是库的分类汇总：

类型分布：{{genres}}
国家/地区分布：{{countries}}
年代分布：{{decades}}
导演：{{directors}}
高频演员：{{cast}}
评分分布：{{ratings}}
制作成本分布：{{budgets}}

当前时间：{{now}}

灵感必须能对应到上述电影库里确实存在的片（主题、类型、年代至少有一项吻合），不要无中生有生成库里没有的风格或时代。

请给出 10 条观影灵感建议。每条包含三部分：
- display_zh：中文氛围化展示文案（15-30 字），用第三人称或场景描写，让用户看到后能产生"对，我就想看这种"的感觉，例如「春日午后，阳光正好，来一部轻松的喜剧片吧！」「深夜需要点刺激？一部悬疑惊悚片让你肾上腺素飙升。」
- display_en：英文氛围化展示文案（10-20 words），与 display_zh 含义对应但用地道英文表达，不要直译，例如 "Sunny afternoon vibes — time for a feel-good comedy!" 或 "Need a late-night thrill? A suspense film to get your pulse racing."
- query：与该灵感对应的中文检索语句（15-30 字），必须用第一人称写，描述"我想看什么样的电影"，可以包含氛围/场景关键词，便于后续检索使用，例如「适合在春日午后氛围下观看的轻松喜剧片」「能让我在深夜肾上腺素飙升的悬疑惊悚片」

灵感要多样化，覆盖不同类型、年代、国家、心情、演员、评分档次和制作规模。
结合当前时间和季节给出应景的建议。

必须严格返回 JSON 数组格式，不要包含任何其他内容（不要用 markdown 代码块包裹）：
[{"display_zh": "...", "display_en": "...", "query": "..."}, ...]

格式要求（严格遵守，否则程序无法解析）：
- 返回一个纯 JSON 对象数组，恰好 10 个元素
- 每个元素是一个对象，包含 display_zh、display_en 和 query 三个字符串字段
- display_zh 是中文展示文案，display_en 是英文展示文案，query 是中文检索语句
- 不要嵌套其他对象，不要添加额外字段
